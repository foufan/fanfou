 
 ---
 import { getCoverImage } from '../../utils/getCoverImage.ts';
 ---
 <section class="relative overflow-hidden bg-gradient-to-b from-primary-50 via-white to-white">
  <div class="absolute inset-0">
    <div class="absolute inset-0 bg-grid-slate-900/[0.04] bg-[size:32px_32px]"></div>
  </div>
  <div class="relative">
    <div class="container mx-auto px-4 py-20 md:py-32">
      <div class="grid md:grid-cols-2 gap-12 items-center">
        <!-- 左侧内容 -->
        <div class="text-center md:text-left space-y-10">
          <div class="space-y-6">
            <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-primary-100 text-primary-800">
              全新上线
            </span>
            <h1 class="text-4xl md:text-6xl font-bold text-gray-900 leading-tight">
              探索、创造 <span class="text-primary-600">分享</span>
            </h1>
            <p class="text-xl text-gray-600 leading-relaxed">
              记录技术成长，分享生活点滴，连接志同道合的朋友
            </p>
          </div>
          
          <div class="flex flex-col sm:flex-row gap-4 justify-center md:justify-start">
            <a href="/blog" 
              class="inline-flex items-center justify-center px-8 py-4 text-base font-medium rounded-xl text-white bg-primary-600 hover:bg-primary-700 focus:outline-none focus:ring focus:ring-primary-300 transform hover:scale-105 transition-all duration-300 shadow-lg hover:shadow-primary-500/25">
              <span>浏览文章</span>
              <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
              </svg>
            </a>
            <a href="/about" 
              class="inline-flex items-center justify-center px-8 py-4 text-base font-medium rounded-xl text-gray-700 bg-white hover:bg-gray-50 border border-gray-200 hover:border-primary-600 focus:outline-none focus:ring focus:ring-primary-200 transform hover:scale-105 transition-all duration-300 shadow-sm">
              了解更多
            </a>
          </div>
 
        </div>

        <div class="relative">
          <!-- 背景渐变层 -->
          <div class="absolute inset-0 bg-gradient-to-tr from-primary-500/20 to-transparent rounded-2xl"></div>
          
          <!-- 主图片 -->
          <img src={ getCoverImage() } alt="Hero" 
            class="rounded-2xl shadow-2xl transform hover:scale-105 transition-transform duration-500 w-full h-[500px] object-cover" />
          
          <!-- 底部右侧模糊元素 -->
          <div class="absolute -bottom-6 -right-6 w-20 h-20 bg-primary-600 rounded-2xl opacity-20 blur-xl"></div>
          
          <!-- 顶部左侧模糊元素 -->
          <div class="absolute -top-6 -left-6 w-28 h-28 bg-primary-400 rounded-full opacity-20 blur-xl"></div>
        </div>
        
      </div>
    </div>
  </div>
</section>
