---
import BaseHead from "../components/BaseHead.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import { SITE_TITLE, SITE_DESCRIPTION } from "../consts";
import { friends } from "../data/friends";

---

<!doctype html>
<html lang="zh-CN">
  <head>
    <BaseHead
      title={`友情链接 - ${SITE_TITLE}`}
      description={SITE_DESCRIPTION}
    />
  </head>
  <body class="bg-gray-50">
    <Header />
    <main>
      <!-- Hero Section -->
      <section
        class="relative py-20 bg-gradient-to-b from-primary-50 via-white to-white overflow-hidden"
      >
        <div class="absolute inset-0">
          <div
            class="absolute inset-0 bg-grid-slate-900/[0.04] bg-[size:32px_32px]"
          >
          </div>
        </div>
        <div class="relative container mx-auto px-4">
          <div class="max-w-3xl mx-auto text-center">
            <span
              class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-primary-100 text-primary-800 mb-6"
            >
              精选推荐
            </span>
            <h1 class="text-4xl md:text-5xl font-bold text-gray-900 mb-6">
              友情链接
            </h1>
            <p class="text-xl text-gray-600">与志同道合的朋友们互相交流学习</p>
          </div>
        </div>
      </section>
 

      <!-- Friends Grid -->
      <section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <div class="grid gap-8 md:grid-cols-2 lg:grid-cols-3">
          {
            friends.map((friend) => (
              <a
                href={friend.url}
                target="_blank"
                rel="noopener noreferrer"
                class="group bg-white rounded-2xl shadow-sm hover:shadow-lg transition-all duration-300 p-6"
              >
                <div class="flex items-center space-x-4">
                  <div class="flex-shrink-0">
                    <img
                      src={friend.avatar}
                      alt={friend.name}
                      class="w-16 h-16 rounded-full object-cover group-hover:ring-4 ring-primary-100 transition-all duration-300"
                    />
                  </div>
                  <div class="flex-1 min-w-0">
                    <h3 class="text-lg font-semibold text-gray-900 group-hover:text-primary-600 transition-colors duration-300">
                      {friend.name}
                    </h3>
                    <p class="text-sm text-gray-500 truncate">
                      {friend.description}
                    </p>
                  </div>
                  <div class="flex-shrink-0">
                    <svg
                      class="w-5 h-5 text-gray-400 group-hover:text-primary-600 transition-colors duration-300"
                      fill="none"
                      stroke="currentColor"
                      viewBox="0 0 24 24"
                    >
                      <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M9 5l7 7-7 7"
                      />
                    </svg>
                  </div>
                </div>
                {friend.tags && (
                  <div class="mt-4 flex flex-wrap gap-2">
                    {friend.tags.map((tag) => (
                      <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-primary-100 text-primary-800">
                        {tag}
                      </span>
                    ))}
                  </div>
                )}
              </a>
            ))
          }
        </div>
      </section>

      <!-- Apply Section -->
      <section class="bg-white border-t border-gray-200">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
          <div class="max-w-3xl mx-auto text-center">
            <h2 class="text-3xl font-bold text-gray-900">申请友链</h2>
            <p class="mt-4 text-lg text-gray-600">
              如果您想与我交换友链，请确保您的网站满足以下条件
            </p>

            <div class="mt-8 bg-gray-50 rounded-2xl p-8">
              <ul class="space-y-4 text-left">
                <li class="flex items-start">
                  <svg
                    class="w-6 h-6 text-primary-600 mt-0.5"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                  >
                    <path
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      stroke-width="2"
                      d="M5 13l4 4L19 7"></path>
                  </svg>
                  <span class="ml-3 text-gray-600"
                    >网站内容健康向上，无不良信息</span
                  >
                </li>
                <li class="flex items-start">
                  <svg
                    class="w-6 h-6 text-primary-600 mt-0.5"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                  >
                    <path
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      stroke-width="2"
                      d="M5 13l4 4L19 7"></path>
                  </svg>
                  <span class="ml-3 text-gray-600"
                    >网站稳定运行，访问速度正常</span
                  >
                </li>
                <li class="flex items-start">
                  <svg
                    class="w-6 h-6 text-primary-600 mt-0.5"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                  >
                    <path
                      stroke-linecap="round"
                      stroke-linejoin="round"
                      stroke-width="2"
                      d="M5 13l4 4L19 7"></path>
                  </svg>
                  <span class="ml-3 text-gray-600"
                    >网站有原创内容，保持一定更新频率</span
                  >
                </li>
              </ul>
            </div>

            <div class="mt-8">
              <a
                href="mailto:your-email@example.com"
                class="inline-flex items-center px-6 py-3 border border-transparent text-base font-medium rounded-md text-white bg-primary-600 hover:bg-primary-700 transition-colors duration-300"
              >
                <svg
                  class="w-5 h-5 mr-2"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"
                  ></path>
                </svg>
                发送邮件申请
              </a>
            </div>
          </div>
        </div>
      </section>
    </main>
    <Footer />
  </body>
</html>
